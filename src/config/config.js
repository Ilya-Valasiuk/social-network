// const LOCAL_HOST = `${window.location.hostname}:3000`;
// const LOCAL_URL_PREFIX = `http://${LOCAL_HOST}/api`;
const LOCAL_HOST = `social-network-api-test.herokuapp.com`;
const LOCAL_URL_PREFIX = `https://${LOCAL_HOST}/api`;


export {
  LOCAL_HOST,
  LOCAL_URL_PREFIX,
};
